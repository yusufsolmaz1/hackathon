ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.hackathon.ApplicationKt.module ]
    }
}

supabase {
    url = ${?SUPABASE_URL}
    key = ${?SUPABASE_KEY}
}
